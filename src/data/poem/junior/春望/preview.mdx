import { Meta } from '@/components/poem-meta';
import { PoemPreview } from '@/components/poem-preview/poem-preview';
import fullData from './full.json';
import { MemorizeContextProvider } from "@/components/poem-preview/memorize-context";
import full2preview from "../../../../../scripts/format/full2json.mjs";

<Meta 
  title={fullData.name}
  author={fullData.author}
  dynasty={fullData.dynasty}
/>

<MemorizeContextProvider>
  <PoemPreview data={{ mode: fullData.mode, preview: full2preview(fullData).preview }} />
</MemorizeContextProvider>

## 写作背景

{fullData.background}

## 作品赏析

{fullData.appreciation}